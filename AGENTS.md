# Agent Instructions

## Pull Requests

Pull requests MUST always target `main` of this repository (`mechanai/spr`). Never create PRs
against the upstream fork (`spacedentist/spr`). When using `gh pr create`, always pass
`--repo mechanai/spr` to ensure the correct target.
